#!/usr/bin/env bash
#Before running this script, run chmod +x scripts/initCommands.sh to make it executable. This only needs to be done once after cloning the repo.
chmod +x cli-nnections/cli-nnections.js
chmod +x wrdli/wrdli.js
chmod +x scripts/populate_dailyWords.js